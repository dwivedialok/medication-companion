# Project name used for resource naming
project_name = "medication-companion"

# Your Google Cloud project id
project_id = "medication-companion-dev"

# The Google Cloud region you will use to deploy the infrastructure
region = "us-central1"

# Human developer who runs `make deploy` from a laptop (uploads drugs.db to GCS).
# Omit or set to "" in CI / shared environments.
dev_deployer_email = "alok.dev.dwivedi@gmail.com"
