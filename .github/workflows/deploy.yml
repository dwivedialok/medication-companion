name: Deploy

# Manual deploy: runs the full CI pipeline (tests + shellcheck) before deploying.
# Automatic deploy on push to main is handled by the deploy job in ci.yml.

on:
  workflow_dispatch:

jobs:
  pipeline:
    uses: ./.github/workflows/ci.yml
    secrets: inherit
