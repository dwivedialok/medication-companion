#!/usr/bin/env bash
# TODO: Remove all GCP resources created by setup_gcp.sh
#
# Stub kept so CI shellcheck covers this path. Implement teardown when a
# full environment destroy is needed (see scripts/setup_gcp.sh for resources).
set -euo pipefail

echo "teardown_gcp.sh is not implemented yet." >&2
exit 1
